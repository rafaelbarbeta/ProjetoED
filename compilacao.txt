Utilizamos a biblioteca check para verificar o funcionamento das duas estruturas de dados.
Para a interface grafica, utilizamos a versao 3 da biblioteca GTK (libgtk-3-dev).

Makefile para compilacao:
Para a compilacao da aplicacao utilizando a libgtk, usar comando "make gtkVigenere" e executar.
Para a compilacao da aplicacao em versão terminal, usar comando "make vigenere" e executar.
Para a compilacao dos testes de estrutura usando a libcheck, usar comando "make testes" e executar.
Para compilacao completa, usar comando "make all".


Para testar a cifra, utilize a ferramenta online : https://cryptii.com/pipes/vigenere-cipher
